# Third-Party Notices

This package does not include or depend on any third-party runtime libraries.
It does not include any Google Play SDK binaries, native Android code, or Gradle dependencies.

All C# code is original work by BizSim Game Studios.

---

## Unity Editor APIs

This package uses Unity Editor APIs (`UnityEditor` namespace) for:

- `PlayerSettings.GetScriptingDefineSymbols` — scripting define management
- `AppDomain.GetAssemblies()` — assembly scanning for package detection
- `EditorWindow` — Package Dashboard UI

These APIs are subject to the
[Unity Software Additional Terms](https://unity.com/legal/terms-of-service/software).
